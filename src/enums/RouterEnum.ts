export enum RouterEnum {
  home = "/",
  login = "/login",
  register = "/register",
  logout = "/logout",
  appointments = "/appointments",
  newAppointments = "/newAppointment",
  notFound = "*",
}
